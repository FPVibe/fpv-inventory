services:
  fpv-inventory:
    image: ghcr.io/cori/fpv-inventory:sha-aa12c72
    ports:
      - "8000:8000"
    volumes:
      - fpv-data:/data
    environment:
      DB_PATH: /data/fpv-inventory.db
      PHOTOS_DIR: /data/photos
      PORT: "8000"
    restart: unless-stopped

volumes:
  fpv-data:
